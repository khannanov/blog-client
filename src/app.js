import React from 'react';
import ReactDOM from 'react-dom';
import ArticleList from './components/ArticleList';

let container = document.getElementById('app');
ReactDOM.render(<ArticleList />, container);